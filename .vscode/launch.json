{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "preLaunchTask": "Build",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/debug/a.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
